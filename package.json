{
  "name": "spinnit",
  "version": "1.0.0",
  "private": true,
  "description": "Spinnit — free random generators and tools",
  "scripts": {
    "build": "eleventy",
    "start": "eleventy --serve",
    "watch": "eleventy --watch",
    "clean": "rimraf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "rimraf": "^5.0.5"
  }
}
