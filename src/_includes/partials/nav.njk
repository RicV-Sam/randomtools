<nav class="nav">
  {%- set localePrefix = '' -%}
  {%- if lang and languages and lang != languages.default -%}
    {%- set localePrefix = '/' + lang -%}
  {%- endif -%}
  <a class="nav-back" href="{{ navBackHref or (localePrefix + '/index.html') }}">← {{ navBackLabel or 'all tools' }}</a>
  <div class="nav-logo">spinn<span>it</span></div>
</nav>
