// Arabic AI pages remain outside the sitemap until translation and
// currentness review is complete. Keep them crawlable for link discovery,
// but prevent indexing until that review explicitly promotes the cluster.
module.exports = {
  noindex: true,
};
